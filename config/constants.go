package config

// ProtocolVersion acts as the single source of truth for the network protocol.
// Bump this when making breaking changes to data structures or API routes.
const ProtocolVersion = "1.0"